-- A script that deletes the database called  hbtn_0c_0
-- If the database hbtn_0c_0 doesn’t exist, the script should not fail
-- Also, not allowed to use the SELECT or SHOW statements

DROP DATABASE IF EXISTS hbtn_0c_0;
